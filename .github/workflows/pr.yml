name: ci

on:
    push:
        branches:
            - master
    pull_request:

jobs:
    lint:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-node@v4
              with:
                  node-version-file: '.nvmrc'
                  cache: 'npm'
            - run: npm ci
            - run: npm run lint

    # Dry-run the email reporting script.
    test-email:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-node@v4
              with:
                  node-version-file: '.nvmrc'
                  cache: 'npm'
            - name: Install Dependencies
              run: npm ci
            - name: Start MongoDB
              uses: supercharge/mongodb-github-action@1.3.0
              with:
                  mongodb-version: 4.4
            - run: npx ts-node -P scripts/tsconfig.json scripts/email.ts
              env:
                  DEBUG: shootismoke:*
                  BACKEND_AQICN_TOKEN: ${{ secrets.AQICN_TOKEN }}
                  BACKEND_MAILGUN_API_KEY: ${{ secrets.STAGING_BACKEND_MAILGUN_API_KEY }}
                  BACKEND_MAILGUN_DOMAIN: ${{ secrets.STAGING_BACKEND_MAILGUN_DOMAIN }}
                  BACKEND_MONGODB_ATLAS_URI: mongodb://localhost/shootismoke

    # Dry-run the expo reporting script.
    test-expo:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-node@v4
              with:
                  node-version-file: '.nvmrc'
                  cache: 'npm'
            - name: Install Dependencies
              run: npm ci
            - name: Start MongoDB
              uses: supercharge/mongodb-github-action@1.3.0
              with:
                  mongodb-version: 4.4
            - run: npx ts-node -P scripts/tsconfig.json scripts/expo.ts
              env:
                  DEBUG: shootismoke:*
                  BACKEND_AQICN_TOKEN: ${{ secrets.AQICN_TOKEN }}
                  BACKEND_MONGODB_ATLAS_URI: mongodb://localhost/shootismoke

    test-unit:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-node@v4
              with:
                  node-version-file: '.nvmrc'
                  cache: 'npm'
            - name: Install Dependencies
              run: npm ci
            - name: Run unit test
              run: npm run test:unit

    test-coverage:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-node@v4
              with:
                  node-version-file: '.nvmrc'
                  cache: 'npm'
            - name: Start MongoDB
              uses: supercharge/mongodb-github-action@1.3.0
              with:
                  mongodb-version: 4.4
            - name: Install Dependencies
              run: npm ci
            - name: Run server, test and generate coverage report
              # The e2e specs read .env too (see test/e2e/backend/jest.config.js),
              # so this one copy configures both the server and the tests.
              run: |
                  cp .env.example .env
                  npm run dev &
                  for i in $(seq 1 60); do
                    curl -fsS --max-time 2 http://localhost:3000/api/health >/dev/null && break
                    sleep 2
                  done
                  npm test -- --coverage
